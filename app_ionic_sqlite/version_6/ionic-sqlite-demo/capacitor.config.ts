import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.batteryreminder',
  appName: 'Battery Reminder',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
